<a href="/links/new">Add link</a>

<ul id="links">
  <% @links.each do |link| %>
    <li>
      <a href="<%= link.url %>"><%= link.title %></a>
        Tag:<a href="<%= link.tags.map(&:name) %>"><%= link.tags.map(&:name) %></a>
    </li>
  <% end %>
</ul>
