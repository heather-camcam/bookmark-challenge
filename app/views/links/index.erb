<a href="/links/new">Add link</a>

<ul id="links">
  <% @links.each do |link| %>
    <li>
      <a href="<%= link.url %>"><%= link.title %></a> <%= link.tags.map(&:name) %>
    </li>
  <% end %>
</ul>
